spring:
  application:
    name: servicio-productos

server:
  port: 8081

eureka:
  client:
    httpclient:
      enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true