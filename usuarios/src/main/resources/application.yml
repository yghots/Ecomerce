spring:
  application:
    name: servicio-usuarios

server:
  port: 8082

eureka:
  client:
    httpclient:
      enabled: true           # ← evita el error de TransportClientFactories
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true